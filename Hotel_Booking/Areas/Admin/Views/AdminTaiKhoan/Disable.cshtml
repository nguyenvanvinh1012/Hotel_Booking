@model Hotel_Booking.Models.TaiKhoan

@{
    ViewBag.Title = "Disable";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header">
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a href="@Url.Action("Index", "Home",new { area = "Admin" })" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Trang chủ</a>
            <a href="@Url.Action("Index", "AdminTaiKhoan",new { area = "Admin" })" class="breadcrumb-item">Danh sách tài khoản</a>
            <span class="breadcrumb-item active">Trạng thái tài khoản </span>
        </nav>
    </div>
</div>

<div class="card-body">
    @if (Model.Active == false)
    {
        <h4 class="card-title">Tài khoản đang bị khóa</h4>
    }
    else
    {
        <h4 class="card-title">Tài khoản đang hoạt động bình thường</h4>
    }

    <div class="table-responsive">
        <table class="product-info-table m-t-20">
            <tbody>

                <tr>
                    <td>Tên tài khoản:</td>
                    <td>@Model.TenTaiKhoan</td>
                </tr>
                <tr>
                    <td>Họ tên:</td>
                    <td>@Model.HoTen</td>
                </tr>
                <tr>
                    <td>Số điện thoại:</td>
                    <td>@Model.SoDienThoai</td>
                </tr>
                <tr>
                    <td>Địa chỉ email:</td>
                    <td>@Model.Email</td>
                </tr>

                <tr>

                    <td>
                        @using (Html.BeginForm())
                        {
                            @Html.AntiForgeryToken()
                            <form asp - action="Disable" asp - route - id="@Model.TenTaiKhoan">
                                <input type="hidden" asp -for="MaTaiKhoan" />
                                @if (Model.Active == true)
                                {
                                    <input type="submit" value="Xác nhận vô hiệu hóa" class="btn btn-danger" />
                                }
                                else
                                {
                                    <input type="submit" value="Xác nhận mở khóa" class="btn btn-success btn-tone m-r-5" />
                                }
                            </form>
                        }
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
